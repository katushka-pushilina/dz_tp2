name: Java CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container: node:20-buster  # Указывает на использование Docker контейнера с Node.js 20

    steps:
    - uses: actions/checkout@v3  # Получение кода из репозитория
    - name: Set up JDK 11
      uses: actions/setup-java@v3  # Установка Java JDK 11
      with:
        java-version: '11'
        distribution: 'temurin'
    - name: Build and Test
      run: |
        javac NumberAnalyzer.java
        javac -cp .:junit-4.13.2.jar:hamcrest-core-1.3.jar NumberAnalyzerTest.java
        java -cp .:junit-4.13.2.jar:hamcrest-core-1.3.jar org.junit.runner.JUnitCore NumberAnalyzerTest
